# DHCP.md
## DHCP (Dynamic Host Configuration Protocol) とは

***

## 概要

* DHCPは、ネットワーク上のデバイスに**自動的にIPアドレスを割り当てるためのプロトコル**
* DHCPは**アプリケーション層(L7)のプロトコル**だが、動作には他のレイヤーも関係しており、特に**トランスポート層(L4)のUDPプロトコル**を利用する
* ポート番号は **サーバー側: 67 / クライアント側: 68**
* ネットワーク管理者が各デバイスに手動でIPアドレスを設定する必要がなくなり、IPアドレスの管理が簡単

## DHCPの主な機能

| 機能 | 内容 |
| --- | --- |
| **IPアドレスの自動割り当て** | DHCPサーバーは、ネットワーク内のクライアントに対して、IPアドレスを自動的に割り当てる |
| **ネットワーク情報の提供** | ネットワークの設定情報（**IPアドレス、サブネットマスク、デフォルトゲートウェイ、DNSサーバーのアドレス**など）も提供する |
| **IPアドレスのリース** | DHCPサーバーは、割り当てたIPアドレスを特定の期間（**リース期間**）だけクライアントに提供する。リース期間が終了すると、クライアントはDHCPサーバーに**IPアドレスの更新を要求**する |

## DHCPの動作の流れ

| 順 | メッセージ | 内容 |
| --- | --- | --- |
| 1 | **DHCP Discover** | クライアントがネットワークに接続すると、DHCPサーバーを探すためにDHCP Discoverメッセージを**ブロードキャスト**する |
| 2 | **DHCP Offer** | DHCPサーバーは、クライアントにIPアドレスを提案するためにDHCP Offerメッセージを送る |
| 3 | **DHCP Request** | クライアントは受け取ったIPアドレスをリクエストするためにDHCP Requestメッセージを送る |
| 4 | **DHCP Acknowledgement (ACK)** | DHCPサーバーはクライアントに対してIPアドレスの割り当てを確認するためにDHCP ACKメッセージを送る |

関連: [well-known-ports.md](well-known-ports.md)（67/UDP・68/UDP）
