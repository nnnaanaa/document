# サブネット計算 チートシート

## 考え方は「区切りを書き出して、間を探す」だけ

割り算で求めなくてOK。ブロック幅の倍数を並べて、対象の数字がどの2つに挟まれるかを見るだけです。

## 手順

1. **ブロック幅を求める**(下の早見表を見るだけ)
2. **区切りを書き出す**: `0, 幅, 幅×2, 幅×3, ...` と256を超えるまで並べる
3. **挟む**: 対象オクテットの値が「どの区切りとどの区切りの間」にあるか探す
4. **下の区切り = ネットワークアドレス** / **上の区切りの1つ手前 = ブロードキャスト**

## 早見表(第4オクテット /25〜/30)

| プレフィックス | ブロック幅 | 区切り |
|---|---|---|
| /25 | 128 | 0, 128 |
| /26 | 64  | 0, 64, 128, 192 |
| /27 | 32  | 0, 32, 64, 96, 128, 160, 192, 224 |
| /28 | 16  | 0, 16, 32, 48, ... 240 |
| /29 | 8   | 0, 8, 16, 24, ... 248 |
| /30 | 4   | 0, 4, 8, 12, ... 252 |

## 例:`220.7.39.150/29`

- ブロック幅は表より **8**
- 区切り: `0, 8, 16, 24, 32, ... 144, 152, ...`
- **150** は `144` と `152` の間 → **144が開始**
- ネットワークアドレス = `220.7.39.144`
- ブロードキャスト = 次の区切り152の1つ手前 = `220.7.39.151`

## 第3オクテットをまたぐとき(/17〜/23)も同じ

対象が第3オクテットになるだけで、やることは同じ。ただし**第4オクテットは丸ごと`0`か`255`**になります。

例:`169.28.203.42/21` → ブロック幅8 → 区切り `..., 200, 208, ...` → **203**は`200`と`208`の間
→ ネットワーク `169.28.200.0` / ブロードキャスト `169.28.207.255`

## 使用可能ホスト数

```
ブロック幅 − 2
```
(区切りの開始と終わりの2つは、ネットワークアドレスとブロードキャストなので使えない)
