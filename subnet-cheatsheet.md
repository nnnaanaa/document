# サブネット計算 チートシート

## 公式

```
ブロック幅       = 2^(32 − prefix)
使用可能ホスト数 = ブロック幅 − 2
```

**ブロック幅の出し方(実務で早いやり方)**: 対象オクテットの**サブネットマスク値を256から引く**。

```
ブロック幅(該当オクテット) = 256 − マスク値
```

例: `/29` → 該当オクテットのマスクは`248` → `256−248=8`
(マスク値自体は「ネットワーク側のビット数だけ1を並べた2進数」を10進数にしたもの。`/29`なら該当オクテットの上位5bitが1→`11111000`=248)

## ネットワーク・ブロードキャストの計算式

```
ネットワークアドレス(対象オクテット) = (対象オクテットの値 ÷ ブロック幅の商) × ブロック幅
ブロードキャスト(対象オクテット)     = ネットワークアドレス + ブロック幅 − 1
```

## 手順

1. **ブロック幅を求める**(早見表 or `256−マスク値`)
2. **割る**: `対象オクテットの値 ÷ ブロック幅` を計算し、商(余りは切り捨て)を出す
3. **掛ける**: `商 × ブロック幅` = ネットワークアドレス
4. **足す**: `ネットワークアドレス + ブロック幅 − 1` = ブロードキャスト

※ 区切り(`0, 幅, 幅×2, ...`)を書き出して該当箇所を探すのも同じ計算の可視化なので、暗算しづらいときはそちらでもOK。

## 早見表(第4オクテット /25〜/30)

| プレフィックス | ブロック幅 | 区切り |
|---|---|---|
| /25 | 128 | 0, 128 |
| /26 | 64  | 0, 64, 128, 192 |
| /27 | 32  | 0, 32, 64, 96, 128, 160, 192, 224 |
| /28 | 16  | 0, 16, 32, 48, ... 240 |
| /29 | 8   | 0, 8, 16, 24, ... 248 |
| /30 | 4   | 0, 4, 8, 12, ... 252 |

## 例:`220.7.39.150/29`

- ブロック幅は表より **8**
- `150 ÷ 8 = 18 余り 6` → 商は **18**
- ネットワークアドレス = `18 × 8 = 144` → `220.7.39.144`
- ブロードキャスト = `144 + 8 − 1 = 151` → `220.7.39.151`

## 第3オクテットをまたぐとき(/17〜/23)も同じ

対象が第3オクテットになるだけで、やることは同じ。ただし**第4オクテットは丸ごと`0`か`255`**になります。

## 早見表(第3オクテット /17〜/23)

| プレフィックス | ブロック幅 | 区切り |
|---|---|---|
| /17 | 128 | 0, 128 |
| /18 | 64  | 0, 64, 128, 192 |
| /19 | 32  | 0, 32, 64, 96, 128, 160, 192, 224 |
| /20 | 16  | 0, 16, 32, 48, ... 240 |
| /21 | 8   | 0, 8, 16, 24, ... 248 |
| /22 | 4   | 0, 4, 8, 12, ... 252 |
| /23 | 2   | 0, 2, 4, 6, ... 254 |

例:`169.28.203.42/21`

- ブロック幅は表より **8**
- `203 ÷ 8 = 25 余り 3` → 商は **25**
- ネットワークアドレス = `25 × 8 = 200` → `169.28.200.0`(第4オクテットは`0`)
- ブロードキャスト = `200 + 8 − 1 = 207` → `169.28.207.255`(第4オクテットは`255`)

## 使用可能ホスト数

```
ブロック幅 − 2
```
(区切りの開始と終わりの2つは、ネットワークアドレスとブロードキャストなので使えない)
