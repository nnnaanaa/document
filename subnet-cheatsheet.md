# CIDRサブネット計算 チートシート

## なぜブロック幅は2の累乗になるのか(導出)

マスクは「ネットワーク部=1、ホスト部=0」の並び。例えば`/29`なら、24ビット(第1〜3オクテット)を使い切った後、第4オクテットは

```
1 1 1 1 1 0 0 0   ← 前半5bitがネットワーク部、後半3bitがホスト部
```

ホスト部が3ビットあると、その3ビットは`000`〜`111`まで`2^3=8`通りの組み合わせを表現できる。これがそのネットワークに含まれるアドレスの個数=**ブロック幅**。だから

```
ブロック幅 = 2^(ホスト部のビット数) = 2^(32−prefix)
```

`/28`ならホスト部4bit→ブロック幅`2^4=16`、`/30`ならホスト部2bit→`2^2=4`、というように決まる。

## 基本公式

| 項目 | 式 |
|---|---|
| ホスト部ビット数 | `32 − プレフィックス長` |
| ブロック幅(アドレス総数) | `2^(32−prefix)` |
| 使用可能ホスト数 | `2^(32−prefix) − 2`(ネットワーク・ブロードキャストを除く) |

## どのオクテットで区切れるか(最重要表)

プレフィックス長を8で割った範囲で、どのオクテットが変化するか・そのオクテット内でのブロック幅が決まる。

| プレフィックス範囲 | 対象オクテット | ブロック幅の式 |
|---|---|---|
| /1〜/8   | 第1オクテット | `2^(8−p)`  |
| /9〜/16  | 第2オクテット | `2^(16−p)` |
| /17〜/24 | 第3オクテット | `2^(24−p)` |
| /25〜/32 | 第4オクテット | `2^(32−p)` |

※ 対象オクテットより**前**は元のIPのまま、**後ろ**はネットワークアドレスなら全部`0`、ブロードキャストなら全部`255`。

## 計算手順(共通)

1. プレフィックス長から対象オクテットとブロック幅を求める(上表)
2. `対象オクテットの値 ÷ ブロック幅` の**商 × ブロック幅** → ネットワークアドレスの対象オクテット
   - 割り切れる(余り0)ときは、その値がそのままネットワークアドレスの値(繰り下げ不要)
3. ネットワークアドレス = そこより後ろを全部 `0` にする
4. ブロードキャスト = 対象オクテットを「ネットワークアドレスの値 + ブロック幅 − 1」にし、後ろを全部 `255` にする
5. 使用可能範囲 = ネットワーク+1 〜 ブロードキャスト−1

## よく使う値の早見表(第4オクテットで区切る /25〜/30)

| プレフィックス | マスク(第4オクテット) | ブロック幅 | 使用可能ホスト数 |
|---|---|---|---|
| /25 | 128 | 128 | 126 |
| /26 | 192 | 64  | 62  |
| /27 | 224 | 32  | 30  |
| /28 | 240 | 16  | 14  |
| /29 | 248 | 8   | 6   |
| /30 | 252 | 4   | 2   |

例: `220.7.39.150/29` → 第4オクテット `150 ÷ 8 = 18 余り6` → `18×8=144`
→ ネットワーク `220.7.39.144` / ブロードキャスト `144+8−1=151` → `220.7.39.151` / ホスト数 `8−2=6`

第4オクテットは「後ろのオクテット」がないので、繰り上げ・繰り下げなしでこの1オクテットの計算だけで完結する(第3オクテット以上とはここが違う)。

## オクテットをまたぐ例(/17〜/23)

| プレフィックス | 対象オクテット | ブロック幅 |
|---|---|---|
| /17 | 第3 | 128 |
| /18 | 第3 | 64  |
| /19 | 第3 | 32  |
| /20 | 第3 | 16  |
| /21 | 第3 | 8   |
| /22 | 第3 | 4   |
| /23 | 第3 | 2   |

例: `169.28.203.42/21` → 第3オクテット `203 ÷ 8 = 25 余り3` → `25×8=200`
→ ネットワーク `169.28.200.0` / ブロードキャスト `169.28.207.255`

## 特殊系

| プレフィックス | 内容 |
|---|---|
| `/31` | RFC 3021。2アドレスとも使用可能(P-to-P回線用)。ネットワーク/ブロードキャストの区別なし |
| `/32` | ホスト経路。1アドレスのみ。ネットワーク=ブロードキャスト=そのアドレス自身 |

## つまずきやすいポイント

- **ホスト数の「2引く」忘れ**: ブロック幅そのものではなく、ネットワークとブロードキャストを除いた数
- **境界に乗っている値の勘違い**: 対象オクテットの値が既にブロック幅の倍数(余り0)なら、そのままの値が正解。1つ下にずらす必要はない
- **オクテットをまたぐ問題での繰り上げ/繰り下げ忘れ**: 対象オクテットより後ろのオクテットは、ネットワークなら`0`、ブロードキャストなら`255`に揃える
